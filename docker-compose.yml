services:
  proxy:
    image: ghcr.io/simba-avionic/srp-app-proxy:latest
    build:
      context: .
      dockerfile: proxy/Dockerfile
    container_name: srp-proxy
    network_mode: host
    volumes:
      - ./desktop/data/csv:/app/desktop/data/csv
      - ./logs:/app/logs
    cap_add:
      - NET_ADMIN
    environment:
      MULTICAST_GROUP: ${MULTICAST_GROUP}
      INTERFACE_IP: ${INTERFACE_IP}
      SD_PORT: ${SD_PORT}
    restart: unless-stopped

  desktop:
    image: ghcr.io/simba-avionic/srp-app-desktop:latest
    build:
      context: .
      dockerfile: desktop/Dockerfile
      args:
        ADMIN_PASSWORD: ${ADMIN_PASSWORD}
    container_name: srp-desktop
    ports:
      - "${DESKTOP_PORT}:80"
    environment:
      PROXY_UPSTREAM: host.docker.internal:5000
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      - proxy
    restart: unless-stopped
