# Skopiuj do .env i dostosuj wartości:
# cp .env.example .env

# --- Proxy / SOME/IP ---
MULTICAST_GROUP=224.224.224.245
INTERFACE_IP=192.168.10.49
INTERFACE_IP_FINAL=10.101.0.1
SD_PORT=30490
NEXT_PORT=10260

# --- Desktop / nginx ---
DESKTOP_PORT=8080
ADMIN_PASSWORD=srp-admin  # wczytywane przy starcie kontenera z .env, bez przebudowy obrazu

# --- Wolumeny (ścieżki na hoście) ---
CSV_DATA_DIR=/home/simba/app_data/csv
LOGS_DIR=/home/simba/app_data/logs
